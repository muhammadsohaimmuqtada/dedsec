# Scan runtime

The scan runtime is the authority for scope, request accounting, shared transport state, evidence identity, and runtime-aware module execution. Presentation and detector code should not duplicate those responsibilities.
