# Compatibility policy

Runtime modernization should preserve the installed `dedsec` command and existing legacy module entry point while the bundled modules migrate.

Breaking changes to command-line flags, report fields, plugin interfaces, or public Python APIs require migration notes and an appropriate package version change.
