# Runtime authority

The shared runtime becomes authoritative only after bundled active HTTP modules have migrated away from direct request calls and legacy helpers. Until then, the branch intentionally preserves compatibility while making the new contract available.
