# Review checklist

- scope checks fail closed
- transport verifies TLS by default
- retries and request counts are bounded
- runtime-aware modules retain legacy compatibility
- verified finding types require evidence
- README and package metadata match behavior
- CI passes across the supported test matrix
