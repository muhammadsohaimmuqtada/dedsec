# Terminology

**Observation**: a fact recorded by a module.

**Candidate**: an observation with possible security relevance that still needs verification.

**Verified finding**: a security finding with explicit verification semantics and evidence references.

**Evidence**: persisted, attributable scan data used to support or reject a finding.

**Scope**: the set of hosts, schemes, and ports the active runtime may contact as target traffic.
