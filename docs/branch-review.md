# Branch review criteria

The branch is ready for a draft PR only after the runtime tests pass, CI is green, redundant documentation has been consolidated, and the diff remains migration-safe relative to current `main`.
