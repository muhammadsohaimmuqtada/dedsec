# Transport implementation status

The shared transport foundation exists on the professionalization branch. Its next implementation step is to attach one transport instance to each scan context so runtime-aware modules share pooling, cache, budget, and scope state.
