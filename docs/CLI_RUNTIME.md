# CLI runtime controls

`--max-requests` defines the shared request budget for runtime-aware modules. `--root-only` narrows runtime-aware target scope from the root domain plus subdomains to the root host only. Legacy modules continue to use the existing compatibility path until migrated.
