# Recommended follow-up pull requests

1. Migrate CORS, redirect, and rate-limit modules to the shared transport and candidate/verified semantics.
2. Add request-level evidence capture and redacted response excerpts.
3. Add normalized asset and endpoint stores.
4. Introduce dependency-aware scheduling for discovery -> profiling -> detection.
5. Remove legacy executors and direct ad-hoc request paths.
