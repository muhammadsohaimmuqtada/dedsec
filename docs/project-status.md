# Project status

The current v2 baseline includes evidence-first reporting and bounded module reliability. The next engineering stage is the shared runtime: scope, transport, request budgets, typed findings, and incremental module migration.

The project should be described as a modular reconnaissance framework under active development rather than claiming complete vulnerability verification across every detector.
