# Core runtime components

The v2 core runtime consists of scope policy, scan context, request budget, centralized transport, evidence storage, typed finding lifecycle models, and orchestrator compatibility hooks.
