# Versioning

DEDSEC uses semantic versioning for the CLI/package and independent schema versions for machine-readable report formats.

A report schema change does not automatically require the package major version to match the schema number. Breaking CLI, plugin, or public Python API changes require an appropriate semantic-version change and migration notes.
