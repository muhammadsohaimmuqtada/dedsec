# Architecture summary

DEDSEC 1.2 keeps the current module API while introducing a shared scan context, fail-closed scope decisions, a shared request budget, a centralized transport foundation, and typed finding lifecycle models. These components are the compatibility layer for the staged v2 migration.
