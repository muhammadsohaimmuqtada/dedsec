# Runtime scope example

For a scan rooted at `example.com`, the default v2 scope allows the root host and its subdomains over HTTP/S. Unrelated hosts are denied unless an explicit allow-list is configured. Explicit deny rules override inherited subdomain allowance.
