# Current branch scope

`agent/dedsec-v2-core-runtime-professionalization` establishes the v2 runtime foundation and repository quality standards. It does not attempt to rewrite every bundled detector in one change.

The intended merge boundary is a tested scope/runtime/transport foundation plus compatibility hooks and professional project documentation. Detector migrations can then be reviewed in smaller follow-up changes.
