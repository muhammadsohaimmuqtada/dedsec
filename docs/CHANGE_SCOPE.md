# Change scope

This pull request establishes the shared v2 runtime foundation and repository quality baseline. It intentionally avoids rewriting every detector at once; detector migration follows after the shared runtime is proven across the supported Python matrix.
