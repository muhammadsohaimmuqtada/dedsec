# Foundation release

Version 1.2.0 is the compatibility-preserving runtime foundation. The package should not be labeled 2.0.0 until the bundled modules have migrated to the shared runtime and legacy request/executor paths are no longer authoritative.
